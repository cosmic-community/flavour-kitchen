import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Next.js 16 no longer supports eslint and typescript options in config
  // ESLint and TypeScript checking are handled via their own config files
}

export default nextConfig